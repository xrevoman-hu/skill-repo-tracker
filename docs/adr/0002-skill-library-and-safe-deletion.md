# ADR 0002：Skill 主库与同步副本安全删除

- 状态：Accepted
- 决策：`skill_library_root` 是事实源；工具目录只是发布目标。只有
  `skill_sync_records` 记录为本应用创建的副本才允许删除或回滚。
- 原因：同名目录可能由用户或其他工具维护，仅凭路径/名称删除会造成数据损失。
- 后果：取消默认目标只影响未来同步；批量应用设置必须先备份，再删除已登记副本；未知
  文件保留并明确报告。
