# ADR 0005：Prompt ZIP 与整库迁移职责分离

- 状态：Accepted
- 决策：Prompt ZIP 面向可选择的提示词内容/标签交换；`.srtmigration` 面向应用非敏感
  状态迁移。两者保持独立 schema 版本域和大小/路径校验。
- 原因：合并格式会扩大敏感数据暴露面，并让局部导入承担整库恢复语义。
- 后果：新字段必须明确属于哪个域；兼容测试分别覆盖旧 ZIP 和历史数据库 fixture，禁止
  将 token、本地源码、源码 ZIP 或任务日志加入迁移包。
