# ADR 0007：热点文件渐进拆分

- 状态：Accepted
- 决策：用机器预算冻结现有热点，先抽 port/adapter 和领域服务，再移动行为；不做一次性
  重写。`App.tsx` 最终小于 700 行，`lib.rs` 最终小于 500 行。
- 原因：当前热点包含大量可工作的边界和回归资产，一次性重写会放大不可审查风险。
- 后果：每个垂直切片先锁定公开 seam 的失败测试，迁移后保持契约不变并下调预算。新
  功能不能让热点增长；新增模块上限 800 行。本文暂时为既有超大模块提供可审查例外。
