interface:
  display_name: "Bug 修复与证据"
  short_description: "Skill Repo Tracker Bug 修复与证据"
  default_prompt: "Use $bugs in the explicitly selected Skill Repo Tracker checkout."
compatibility:
  canonical_format: "agent-skills"
  adapter_targets: ["openai"]
  activation:
    mode: "manual"
    paths: []
  execution:
    context: "inline"
    shell: "bash"
  trust:
    source_tier: "local"
    remote_inline_execution: "forbid"
    remote_metadata_policy: "allow-metadata-only"
  degradation:
    openai: "Repository-bound instructions; host permissions remain authoritative; metadata is not native enforcement."
